replicaCount: 1

image:
  repository: ghcr.io/<GITHUB_OWNER>/<REPO_NAME>/helloapi
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

# ---- exigido pelos templates padrão do Helm ----
serviceAccount:
  create: true
  annotations: {}
  name: ""

ingress:
  enabled: false

podSecurityContext: {}
securityContext: {}

autoscaling:
  enabled: false

resources: {}
nodeSelector: {}
tolerations: []
affinity: {}
